# Traceability Matrix

Aligned: PRD 1.2 → HLD 1.2 → LLD 1.10 (CR-0002). Regenerated by `/propagate-prd`; edit by hand only to add code/test columns.

| PRD | Status | HLD | LLD | Code (module) | Tests |
|---|---|---|---|---|---|
| PRD-F-001 | Active | HLD-004, HLD-C-06 | LLD-RT-04 | runtime/triage.py, runtime/orchestrator.py (route + graceful LLM-junk degradation), runtime/schemas.py, runtime/llm.py, runtime/prompts.py; migrations/versions/0004_ops.py; cli `chat`\|`prompt` | tests/runtime/test_triage.py, tests/runtime/test_runtime_llm.py, tests/runtime/test_prompt_activate_gate.py, tests/runtime/test_llm_hiccup_degrades.py |
| PRD-F-002 | Active | HLD-C-07 | LLD-AG-01 | runtime/agents/application_discovery.py (one-slot fill; answers from the top match's own row fields; family-scoped retrieval; per-match near-edge; no-match → engineer offer; **slot-guard-conversion (this branch): pre-match token guard nulls an over-inferred `lubricated`/`standard` the visitor never stated — scans current message + prior user turns; Latin+Devanagari lubrication tokens, Latin standard codes (short EN/IS/BS/BIS case-sensitive); strip events traced fail-open; closes the wrong-but-nonempty-match path the empty-match relaxation retry misses (turn 8fdc353f) — rule 6 flag**; **followup-carryforward (this branch): deterministic follow-up carry-forward — `_merge_followup` merges newly extracted slots over the session's prior `match_capability` args (`_prior_duty` via `ToolRunner.session_id`; full restatement inherits nothing; unitless new flow asked not guessed; token guards run after merge; answer restates the merged duty); ask_slot messages carrying spec-numbers replaced by the deterministic template; a "no results/tool-failed" answer despite a match (turn 74d45612) recomposed once then honest fallback (denial detection, not brittle exact-name) — rule 6 flag**), runtime/agents/base.py (tool-context render; **renders `match_capability` `converted_capacity` so the compose reconciles units from a sourced figure**), runtime/tools_registry.py (**ToolRunner.session_id**), runtime/orchestrator.py (**n_agent passes session_id**); **station-per-area-grounding (this branch): `APPLICATION_SCHEMA.additional_areas` (one prompt facts-block line + `_extract_slots` key; kept out of `_REQUIRED`/`_DUTY_KEYS` so it never gates match_capability) drives the same `ground_additional_areas` per-area fetch on the answer path — a follow-up "…for a hydrogen refuelling station" fetches + cites the fuelling family; fail-open, skips traced — rule 6 flag** | tests/agents/test_discovery_slots.py (**+ token-matrix guard …**; **+ carry-forward / gas-override / full-restatement / unitless-followup / ask_slot-template-swap / denial-backstop-recompose-then-fallback / guards-after-merge**), tests/agents/test_discovery_answer_from_row.py |
| PRD-F-003 | Active | HLD-002, HLD-C-05, HLD-C-07 | LLD-TOOL-01, LLD-AG-01 | tools/match_capability.py (gas alias map + null-lubricated caveat; **matches / non_comparable_candidates split + per-match near_edge**, M5a live-run fix — rule 6 flag for the doc pass), extract/normalise.py (extended unit vocabulary), client_config.py, clients/jyotech/config.yaml (`gas_aliases`, **M5b: biogas variants**); **M5b: application_discovery relaxes over-inferred standard/lubricated filters on empty match; structural slot-complete rule (re-extract when the message states a full duty); same-unit family ranking — a query in a family's native unit outranks one reached by conversion**; **slot-guard-conversion (this branch): matches additively carry `converted_capacity {value, unit, from}` when reached by unit conversion (the already-computed `conv_capacity`, **rounded to a whole unit for presentation (followup-carryforward) so the compose's reconciliation figure is sourced**; only when `not same_unit`) — result-shape only, no matching/ranking/exclusion change; persona no-arithmetic rule keeps the compose from deriving figures itself — rule 6 flag** | tests/tools/test_match_capability.py (incl. hydrogen-3000 regression; **+ converted_capacity exposed on cross-unit match / omitted on same-unit**), tests/extract/test_normalise.py |
| PRD-F-004 | Active | HLD-C-07 | LLD-AG-02, LLD-TOOL-02, LLD-TOOL-03 | tools/list_products.py, tools/get_product.py (alias norm + null-model display + **containment fallback: a ≥4-char published name embedded in a phrase resolves — rule 6 flag**); **agent done (M5b)**: runtime/agents/product_advisor.py (precise parse; for a named model: get_product + **family-scoped search_documents so catalogue specs (F.A.D./pressures/drive) are composed with the row fields**; force-cite the structured source; exact model names + family summary; null-model → family+variant; price/lead-time → commercial handoff; English retrieval query); grounding cites the product's source document; **presentation-shapes (this branch): explicit-comparison multi-fetch — `PRODUCT_QUERY_SCHEMA.compare_items` (schemas.py) + product_advisor.py fetches each named item (get_product per product-level item, scoped retrieval to its family; list_products grounds family-level items that carry capability rows but no `facts.product` rows) with a targeted retrieval per item, force-citing each compared family (a citation per item); a broad division/range ask composes a clustered orientation, no spec recitation; persona table rule reconciled — table at explicit 2+ compare, prose-siblings only for single-product variants — rule 6 flag**; **capability-envelope-lookup (this branch): get_product family-envelope fall-through — a capability-only family (capability rows, no `facts.product` rows) resolves by name (same alias/containment rules, against `facts.active_product_family`) to its published envelope taken verbatim from `facts.active_capability_row` (+ capability_gas); product-backed lookups byte-identical (fires only on the 0-row fall-through); a resolved family with no rows returns `capabilities: []`; product_advisor standard path uses the envelope as the structured source + scopes retrieval to it (compare path unchanged — reads `products` only); render_tool_context renders the per-row envelope via format_number — rule 6 flag**; **station-per-area-grounding (this branch): get_product now resolves the containment fallback (both the product and family-envelope sites, unified) by TOKEN-COVERAGE not longest-substring — a word-preserving tokeniser (`_tokens`, plural-folded) scores each family's name+product tokens and returns the unique Pareto-frontier family (confident), 2–4 co-maximal families (`matched_by:"ambiguous"` + `candidates`), or none (>4 or 0); fixes the wrong-record class ("Smart MCH-16"→mch_smart, not the engine) and the unresolved partial ("natural gas compressors"→natural_gas_recip envelope). product_advisor: `PRODUCT_QUERY_SCHEMA.additional_areas` (one parse line) drives a per-area multi-fetch (base.py `ground_additional_areas`, cap 3, fail-open, family-scoped retrieval, force-cite — mirrors the compare path); an ambiguous PRIMARY name asks ONE clarify listing the candidate display names (LLM-phrased, `action:"clarify"`); ambiguous additional_areas skip + trace (one-question-per-turn). No DB migration (rides ops JSON) — rule 6 flag** | tests/tools/test_tools.py (**+ family-envelope resolve by name/id, empty-envelope family, product-match-no-envelope, unknown-no-envelope**; **+ token-coverage: confident dominator, Exhibit-C dangling-token reject (Smart/Ergo MCH-16), 2–4 ambiguous candidates, >4 no-match, product-variant tokens, no-shared-token, phrase→family**), tests/agents/test_product_advisor.py (**+ compare multi-fetch …; + ambiguous-primary asks clarify with candidate names, no fetch-through**), tests/runtime/test_additional_areas.py (**per-area fetch: confident family-scoped search, ambiguous skipped+traced (not asked), unresolved skipped, fetch-error fail-open, cap-3, dedup**), **tests/runtime/test_render_tool_context.py (envelope figure+unit inseparable; empty-envelope no figures)**; golden regression `prod-mch16-specs`; **golden `prod-compare-process-natgas` (cross-family table) / `prod-overview-industrial` / `prod-overview-industrial-hi` / `cap-false-memory-product` / `cap-station-hydrogen-fuelling` (per-area application fetch, luck-proof cap citation) / `prod-compare-smart-ergo-mch16` (variant resolution)** |
| PRD-F-005 | Active | HLD-C-07 | LLD-AG-03, LLD-TOOL-04/05/06 | tools/search_documents.py (now returns chunk `title`), tools/get_company_fact.py, tools/get_office.py; **agent done (M5b)**: runtime/agents/documents_compliance.py (document_card kind + URL-derived title fallback; verbatim company facts; **polish pass: one card PER DISTINCT DOWNLOADABLE document the answer references — used_documents + file-URL filter, dedup/order/cap 3, k=10 — so a generic catalogue ask cards every catalogue, a specific ask one — rule 6 flag**) | tests/tools/test_search_documents.py, tests/tools/test_tools.py, tests/agents/test_documents.py (**+ generic multi-card turn**) |
| PRD-F-006 | Active | HLD-C-07, HLD-C-08 | LLD-AG-04, LLD-HO-02, LLD-HO-03 | **intake done (M5b)**: runtime/agents/after_sales_intake.py (one-slot/turn, no diagnosis, handoff naming the region office; **adversarial round: collects the contact DETAIL (email/phone) before close, intake output carries all M6 needs**; stub close — no ops.lead); handoff/routing.py (region routing) + email are M6 | tests/agents/test_after_sales_intake.py; tests/handoff/test_region_routing.py pending (M6) |
| PRD-F-007 | Active | HLD-C-07 | LLD-AG-05 | **done (M5b)**: runtime/agents/commercial_routing.py (always handoff, lead_type commercial\|dealer, number-free language templates — no price/lead time) | tests/agents/test_commercial_never_prices.py |
| PRD-F-008 | Active | HLD-C-04, HLD-C-06 | LLD-RT-05, LLD-RET-01/02/03/04, LLD-DB-03 | retrieval + **grounding/citations done (M5a)**: retrieval/chunk.py, retrieval/embed.py, retrieval/tokenizer.py, tools/search_documents.py (hybrid RRF), cli chunk run \| release embed; runtime/grounding.py (citation + numeric guard **applied to EVERY outgoing message in orchestrator.n_ground, not just answers — a spec figure absent from tool results/user is stripped whatever the action, closing the asked_slot gate-bypass**; **M5b: honest parent-derivation of cited results — a cited chunk's document, a matched row's family, a product's family — rule 6 flag**; **polish pass: used_documents() cites EACH distinct document the answer used, not just the single best chunk — a generic catalogue answer cites every catalogue it drew on; single-document answers unchanged — rule 6 flag**; **capability-envelope-lookup (this branch): `_citations_from_record` derives family + capability + document citations from a get_product family-envelope result (honest-parent enrichment of the cited result; numeric/citation gate semantics unchanged) — rule 6 flag**), runtime/agents/base.py (extra_messages), runtime/ops.py (ops.citation); **sources resolver (this branch): runtime/sources.py `resolve_sources(conn, citations)` walks each citation to its source document via facts.active_* and groups into a customer-ready {title,url,link,location,kind} list (PDF page anchors / web section labels; URL-derived title fallback relocated here from documents_compliance); orchestrator.n_ground persists it to ops.message.payload.sources (fail-open, no schema change — presentation rider, rule 6 flag)** | tests/retrieval/test_chunk.py, tests/retrieval/test_embed.py, tests/tools/test_search_documents.py; tests/runtime/test_citations.py (**+ used_documents multi-doc**), tests/runtime/test_hydrogen_turn.py, tests/runtime/test_sources.py (**resolver grouping/dedupe, page-anchor, null-title/null-url, parent-document resolution**), tests/runtime/test_ground_sources.py (**n_ground payload merge + fail-open**) |
| PRD-F-009 | Active | HLD-004, HLD-C-06 | LLD-RT-05 | runtime/grounding.py (citation-backed + numeric spec guard; per-claim NLI deferred, note 2); **polish pass: persona Markdown formatting keeps every figure adjacent to its unit (one cell / one bold span) so the numeric guard is unaffected by `*`/`|`**; **Hindi support (this branch): Devanagari digits (०-९) folded 1:1 to ASCII before the numeric guard (`_fold_digits` in `_all_numbers`/`_spec_numbers`/redaction) so a Devanagari figure can't slip past; `ground_answer(language=…)` ships the fallback sentence in the visitor's language (LLD-RT-07)** | tests/runtime/test_ungrounded_claims_removed.py (**+ markdown-safe `spec_numbers`/`all_numbers`/redaction**; **+ Devanagari digit-fold, per-language fallback (`hi`/`hinglish`), Hindi full-turn fallback**) |
| PRD-F-010 | Active | HLD-C-09, HLD-C-08 | LLD-UI-04, LLD-HO-01 | widget/, handoff/service.py | e2e/test_talk_to_engineer.spec.ts |
| PRD-F-011 | Active | HLD-003, HLD-C-08 | LLD-HO-01…05, LLD-DB-05 | handoff/service.py, handoff/mailer.py | tests/handoff/test_lead_and_dispatch.py |
| PRD-F-012 | Active | HLD-C-06 | LLD-RT-07 | **done (M5b)**: runtime/language.py (`respond_in` + `english_query` — Hindi/Hinglish retrieval query translated to English; tool args + citations stay English), runtime/triage.py (detects language), agents thread respond_in; retrieval query translated in free-text-search agents; **adversarial round: per-turn emphatic response-language (current message wins over history)**; **voice polish: shared persona block clients/jyotech/prompts/_persona.md (Hinglish register rules + advisor voice) prepended by runtime/prompts.py to the six customer-facing agents; per-agent EN/Hinglish exemplars**; **polish pass: persona Markdown formatting (bold display names; small tables for 3+ variants / per-type spec sets, prose otherwise) + source-voice rule (never name a document/catalogue/page in prose — provenance rides in structured citations; exception for document-download / "which source?" asks); leak fixes in product_advisor/application_discovery/faq_company/documents_compliance exemplars**; **Hindi (Devanagari) support (this branch): _persona.md Devanagari register (ASCII-digit figures rule) + Devanagari exemplar in all six agents; triage.md Devanagari→`hi` clause; fixed customer-facing strings selected per-language (en/hi/hinglish) in code — grounding `_FALLBACK_TEXTS`, orchestrator `_CLARIFY_TEXTS`/`_HICCUP_TEXTS`, application_discovery `_NOMATCH_TEMPLATES`, product_advisor `_PRICE_HANDOFF_TEXTS` — never runtime-LLM-translated; 4 English-only residuals in after_sales_intake/application_discovery slot paths documented (docs/changes/hindi-support-notes.md), rule 6 flag**; **v2 register-purity pass: _persona.md drops the enumerated forbidden-word list (positive rule) + adds the tool-vocabulary translate-don't-copy glossary; `respond_in("hi")` now returns a two-line written-Devanagari register reminder (recency at compose); all five fixed `hi` templates (`grounding._FALLBACK_TEXTS`, orchestrator `_CLARIFY_TEXTS`/`_HICCUP_TEXTS`, `application_discovery._NOMATCH_TEMPLATES`/`_NOMATCH_NOTE`, `product_advisor._PRICE_HANDOFF_TEXTS`) re-authored to the three-exception rule — the deferred follow-up is closed**; **v2.1: symmetric `respond_in` — `en`/`hinglish` are now firm last-position instructions of equal strength to `hi` (en forbids Hindi/Hinglish; hinglish pins the romanised register, no Devanagari / no pure-English drift), all three sharing one "reply language comes ONLY from this instruction" tail — fixes a live regression (turn `b8b63a4f`) where an English turn recited the Hinglish exemplar; every agent exemplar is language-tagged ("… use this style ONLY when replying in English") + a `_persona.md` rule to imitate only the example matching the reply language**; **presentation-shapes (this branch): `_persona.md` Devanagari register hardened — transliterate gas names / attribute values / descriptive words (never leave Latin), and never append a parenthetical English gloss after a Hindi word (`क्षमता`, not `क्षमता (capacity)`); **per-language thinking knob (UNCERTIFIED, default off)** — when `LLM_DISABLE_THINKING` is set it disables compose thinking for `en`/`hinglish` only, `hi` always thinks (Devanagari register needs compose reasoning): `runtime/llm.py::compose_thinking_off` + `runtime/agents/base.py` stamps reply language on the compose seam; knob=1 not certified (rotating `en` gate failures) so must not be defaulted — M7 follow-up — rule 6 flag** | tests/runtime/test_language.py (**v2.1: all three firm strings + the shared language-only tail; case-insensitive/None/unknown→English**), **fixed-template `hi` register-purity assertions in tests/runtime/test_ungrounded_claims_removed.py + test_llm_hiccup_degrades.py + tests/agents/test_discovery_answer_from_row.py + test_product_advisor.py + test_commercial_never_prices.py (shared `script_purity_offenders`)**, tests/runtime/test_hinglish.py (routing + language tag + full-slot Hinglish answer with citation discipline), tests/runtime/test_format.py, **tests/runtime/test_llm_hiccup_degrades.py (clarify/hiccup in `hi`), tests/agents/test_product_advisor.py (price handoff in `hi`), tests/agents/test_discovery_answer_from_row.py (no-match handoff in `hi`/`hinglish`)**, **tests/runtime/test_runtime_llm.py (presentation-shapes: per-language thinking gate — `compose_thinking_off` matrix, `hi` keeps thinking on, seam reads `compose_language`, compose stamps the reply language)** |
| PRD-F-013 | Active | HLD-C-07 | LLD-AG-06 | runtime/agents/faq_company.py (company facts + docs; **adversarial round: absence protocol — search before conceding; multi-part decomposition across founder/founded/coverage kinds; citation hygiene — cite only used rows**), runtime/agents/base.py (`compose_grounded_answer` search_fallback), runtime/agents/deflect.py (**M5b: out-of-scope only — the 5b holding path is gone now all six intents route to their agent**) | tests/agents/test_deflect.py, tests/agents/test_faq_company.py, tests/runtime/test_routing.py |
| PRD-F-014 | Active | HLD-001, HLD-006, HLD-C-01, C-03 | LLD-ING-01…05, LLD-REL-01…05, LLD-DB-02 (+ `ingest verify`: new capability, fold into LLD-ING via /prd-change) | ingest/ (done: sources/crawl/html_convert/pdf_convert/finish/patches/staging_write/pipeline/verify.py + cli ingest run|verify), release/ (schema+seed done: migrations/versions/0001_init.py, release/seed.py; **3b LLD-REL-02…05 done**: import_review, check, diff, promote, curated.py; clients/jyotech/seeds/region_state.yaml; migrations/versions/0003_product_model_name_nullable.py; cli release import\|check\|diff\|promote\|activate\|rollback) | tests/ingest/ (test_html_convert, test_pdf_convert, test_finish, test_pipeline, test_sources, test_report_flags, test_patches, test_verify, test_cleaner_rules), tests/db/test_migration_roundtrip.py, tests/release/ (test_import, test_check, test_diff, test_promote) |
| PRD-F-015 | Active | HLD-006, HLD-C-02 | LLD-EXT-01…10, LLD-REL-01, LLD-DB-06 (+ staging markers `conflict_group`/`needs_family`/`section_id` + `staging.release_candidate` ledger: clarification, fold into LLD-DB-06 via rule 6) | extract/ (done: llm, markdown, classify, families, evidence, extractors, normalise, staging_write, registry, prompts, run, report), clients/jyotech/{schemas.py,prompts/}, release/ (candidate, export), migrations/versions/0002_extract_staging.py, cli extract run \| release create\|list\|export; **3b review gate now end-to-end**: release/ (import_review, check, diff, promote); carry-over fixes (release/export `_cell` blank attributes, clients/jyotech/schemas.py gas evidence keeps source case, tools/match_capability non-comparable units) | tests/extract/ (test_extractor_endpoint, test_evidence_required, test_markdown, test_classifier, test_normalise, test_frozen_family, test_rc_idempotency, test_extract_integration), tests/release/ (test_release_candidate, test_export, test_import, test_check, test_diff, test_promote) |
| PRD-N-001 | Active | HLD §5 | LLD-RT-03, LLD-RET-03 | — | perf/test_turn_latency.py |
| PRD-N-002 | Active | HLD-005 | LLD §0 (endpoints), LLD-EXT-02/03/04, LLD-RET-02, LLD-RT | config.py (`extractor_endpoint()` + embed + `llm_*` settings), extract/llm.py, retrieval/embed.py (self-hosted `EMBED_BASE_URL`), retrieval/tokenizer.py; **runtime (M5a)**: runtime/llm.py (self-hosted `LLM_BASE_URL`, never the extractor) | tests/extract/test_extractor_endpoint.py, tests/retrieval/test_embed.py (endpoint-separation test), tests/runtime/test_runtime_llm.py |
| PRD-N-003 | Active | HLD-001, HLD §5 | LLD-DB-04, LLD-HO-01 | **ops schema done (M5a)**: migrations/versions/0004_ops.py (full ops.* incl. message kind), runtime/ops.py (insert-only writers); consent gate M6 | tests/db/test_ops_append_only.py, tests/db/test_migration_roundtrip.py, tests/runtime/test_turn_append_only.py; tests/handoff/test_consent_gate.py pending (M6) |
| PRD-N-004 | Active | HLD-007, HLD §5 | LLD-UI-01, clients/ registry | clients/jyotech/ | tests/test_no_client_strings_in_core.py |
| PRD-N-005 | Active | HLD-008, HLD-009, HLD-C-10 | LLD-EVAL-01…03 | eval/runner.py (**M5b: e2e layer — each question through the real orchestrator + runtime LLM in a rolled-back savepoint; scores outcome / contains / `must_not_contain` / `expected_source_ids ⊆ citations` / handoff-number guard; comma-insensitive number match; per-layer + runtime report — rule 6 flag**), clients/jyotech/golden/questions.yaml (51 questions + must_not_contain), cli eval run `--layers`; **voice polish: runtime/format.py (Indian number grouping), eval/runner.py `normalize_digits`/`text_contains` (digit-typography-insensitive matching)**; golden gate (incl. e2e) wired into `release activate` and `prompt activate` (`--layers`): runtime/prompts.py (`activate_prompt_gated`); **polish pass: source-voice `must_not_contain` (["Source:", "catalog", "catalogue", "page)"]) on co-certifications / faq-api618 / prod-mch16-specs (non-document questions only)**; **Hindi support (this branch): `normalize_digits` folds Devanagari numerals to ASCII; golden suite 53 → 55 (e2e-only `e2e-hindi-hydrogen` + `e2e-hindi-price`, each with a Devanagari-script anchor)**; **v2: opt-in per-question `script_purity: hi` flag in eval/runner.py (`script_purity_offenders` — strips `[tr]` markers / `**bold**` names / `{placeholders}` / standard codes / units, then any remaining `[A-Za-z]{2,}` fails and is named; runs inside `_e2e_attempt` so retry-once applies), enabled on the two e2e-hindi goldens**; **v2.1: romanised-Hindi function-word `must_not_contain` direction guards (`aapki`/`aapke`/`aapko`/`hamari`/`chahiye`/`kya main`) on the English e2e goldens cap-hydrogen-process / prod-mch16-specs / co-certifications — guards the Hinglish reply direction, complementing script_purity's Devanagari guard so every reply language has a golden asserting the others don't appear uninvited**; **presentation-shapes (this branch): golden suite 55 → 58 — `prod-overview-industrial` (overview spec-figure `must_not: 25000` enforces the no-spec-dump shape), `prod-compare-process-natgas` (cross-family comparison — `contains: ["|","process","natural"]`, the pipe proves a Markdown table rendered), `prod-overview-industrial-hi` (clustered overview in Devanagari; shape-guarded by a Devanagari anchor + `must_not: 25000` — `script_purity` deliberately not asserted on a broad overview, a large register-leak surface) — rule 6 flag**; **capability-envelope-lookup (this branch): golden suite 58 → 59 — `cap-false-memory-product` (product-route false-memory twin of `cap-false-memory`; parse extracts the named family so the route exercises get_product's envelope; `fact` block pins get_product resolving the family to its envelope ids; full-figure adoption `must_not` to avoid substring collisions) — rule 6 flag**; **presentation-intelligence (this branch): golden suite 59 → 60 — `faq-founded-and-founder` (two-part → ordered-prose shape guard: founder NAME + loose `199` year handled exactly as green `co-founded`, so the conflicted 1990/1991 figure is never strictly asserted; `must_not "|"` proves prose not a table), and `co-founded` strengthened with `must_not "|"` (single-fact → no-structure guard). The spec-block and two-part-ORDERING shapes stay live-check-guarded (a positive deterministic anchor would need OR-semantics in the runner's contains matching — flagged as a possible M7 eval enhancement, not built here) — rule 6 flag**; **station-per-area-grounding (this branch): golden suite 60 → 62 — `cap-station-hydrogen-fuelling` (e2e-only; application phrase → per-area fetch of the fuelling family; `expected_source_ids` include the capability-row id `cap.doc_hydrogen_compressors_and_fueling_system_s000.1`, luck-proof — cited only if the fetch fired; contains "J2601") and `prod-compare-smart-ergo-mch16` (e2e-only; variant name resolution, Exhibit C; `expected_source_ids` the two DISTINCT variant families `fam.mch_smart_bac`+`fam.mch_ergo_bac`, `must_not "same family"`, `"|"` proves a table) — rule 6 flag** | tests/eval/test_runner.py (e2e pass/fail, must_not_contain, activation blocked on e2e failure; **+ markdown-safe `text_contains`/`normalize_digits`**; **+ Devanagari `normalize_digits`**; **v2: `script_purity_offenders` unit tests (pure passes; stray English named; bold names / units / codes / digits / `[tr]` markers never trigger) + e2e-wiring tests (a Latin leak in a `script_purity: hi` reply fails and is named; a pure reply passes)**), tests/runtime/test_prompt_activate_gate.py + the golden suite itself |
| PRD-N-006 | Active | HLD §5 | — | infra/ | — |

Alignment check (must all be true): every Active PRD id has ≥1 HLD id ✔ · every HLD-C id has ≥1 LLD id ✔ · no LLD id cites a Withdrawn PRD id ✔
