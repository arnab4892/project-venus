---
cr: CR-0001
title: After-sales routes to published branch-office emails
opened: 2026-08-23
author: Arnab Sharma (Nyalazone)
status: Propagated
prd_from: 1.0
prd_to: 1.1
hld_to: 1.1
lld_to: 1.5
---

# CR-0001 — After-sales routes to published branch-office emails

## 1. Why
Jyotech's website contact page publishes branch-office email addresses, so the
original assumption behind after-sales routing no longer holds. Rather than send
every after-sales enquiry to sales@ with the region only in the subject line
(the interim workaround in assumption A-1), the agent should route directly to
the published branch-office email for the customer's region, copying sales@.
This gets service/spares/AMC enquiries to the people who can act on them fastest
and directly supports goal G-3 (route after-sales correctly).

## 2. PRD change
Modify only — no requirement id added or withdrawn. Version bump MINOR: PRD 1.0 → 1.1.

| Requirement | Change | Before | After |
|---|---|---|---|
| PRD-F-006 | Modified | "…and route to the regional office for the customer's state, copying sales@. It shall not give troubleshooting advice." | "…and route the enquiry to the published branch-office email for the customer's region (copying sales@); where no branch-office email is published it shall fall back to sales@ with the region in the subject. It shall not give troubleshooting advice." |
| A-1 (assumption) | Updated | "Regional office emails are not printed publicly; after-sales routes to sales@ with region in the subject until Jyotech supplies them." | "Branch-office emails are published on the website contact page; after-sales routes to the branch-office email for the customer's region (copying sales@), falling back to sales@ with region in the subject only where no office email is published." |

What changed: the routing target for after-sales handoff moves from "sales@ with
region in subject" (interim) to the published branch-office email for the region
(To:), with sales@ as a copy and the old behaviour retained only as a fallback
when no office email exists. The collect-slots and no-troubleshooting clauses of
PRD-F-006 are unchanged.

## 3. Impact analysis (filled by /propagate-prd)

Traceability for the changed requirement: **PRD-F-006 → HLD-C-07, HLD-C-08 →
LLD-AG-04, LLD-HO-02, LLD-HO-03** (Code: `runtime/agents/after_sales.py`,
`handoff/routing.py`; Tests: `tests/handoff/test_region_routing.py`).

| HLD item | Impact | Proposed change |
|---|---|---|
| HLD-C-08 (Handoff service) | Modify (wording) | Its "region routing" responsibility becomes explicit: **route after-sales to the published branch-office email for the region, falling back to sales@**. No component added/removed. `Satisfies` (PRD-F-006, F-010, F-011) unchanged. |
| HLD-C-07 (Intent sub-agents) | None | The After-sales Intake sub-agent still only collects slots and hands off; recipient selection lives in HLD-C-08. Still satisfies PRD-F-006. |
| HLD-003 (lead capture + email dispatch in scope) | None | Unaffected. |

| LLD item | Impact | Proposed change |
|---|---|---|
| LLD-HO-03 (`route_to` + subject) | **Modify** | For `lead_type=after_sales`: `to` = the published branch-office email for the region — `office.email` of the office `region_state(state).office_id`; `cc` = client `handoff_to.sales`. When no branch-office email is published, fall back to `to` = `handoff_to.sales` with the region in the subject. Other lead types keep `to` = `handoff_to.sales` (+ office email if non-null). Subject format unchanged. |
| LLD-HO-02 (region resolution) | None | The `region_state(state)` lookup already returns the row's `office_id` (LLD-DB-07); LLD-HO-03 consumes it. Region inference unchanged. |
| LLD-AG-04 (after_sales_intake) | None | Still collects model/serial/site/need/contact and hands off with `lead_type=after_sales`; no recipient logic in the agent. |

No LLD id is added or withdrawn; no HLD/LLD item is left citing a Withdrawn requirement (this CR is a Modify only).

Data model: no change — `facts.office.email` and `facts.region_state.office_id`
(→ office) already exist (LLD-DB-07). `design/data-model.md` unchanged.

Code and tests affected:
- `handoff/routing.py` — recipient selection: after-sales `to` = branch-office email, `cc` = sales@, with the sales@-fallback (LLD-HO-03). **Modify.**
- `handoff/mailer.py` — must support a `cc` recipient (To vs Cc) for LLD-HO-04 dispatch. **Modify (likely).**
- `runtime/agents/after_sales.py` — no change (agent only collects + hands off).
- `tests/handoff/test_region_routing.py` — assert after-sales `to` = region's branch-office email and `cc` = sales@, plus a **fallback** case (no office email → `to` = sales@, region in subject). **Update + add case.**

## 4. Resulting versions
PRD → 1.1 · HLD → 1.1 (MINOR — HLD-C-08 wording; no component added/removed) ·
LLD → 1.5 (MINOR — LLD-HO-03 modify; no id added/removed) · TRACEABILITY
regenerated ✔ (Phase B).

## 5. Implementation
Implementation tasks (one per affected LLD id, each with its test — write the
test first, per CLAUDE.md). Code is NOT written by `/propagate-prd`.

- [ ] **LLD-HO-03** — `handoff/routing.py`: after-sales recipient selection.
      `to` = branch-office email (`office.email` of `region_state(state).office_id`),
      `cc` = client `handoff_to.sales`; when no branch-office email, fall back to
      `to` = `handoff_to.sales` with the region in the subject. Other lead types
      unchanged.
      *Test:* `tests/handoff/test_region_routing.py` — after-sales resolves to the
      region's branch-office email as `to` and sales@ as `cc`; **add a fallback
      case** (office email null → `to` = sales@, region in subject).
- [ ] **LLD-HO-04 (supporting)** — `handoff/mailer.py`: send with a distinct `cc`
      recipient (To vs Cc) so the above routing is delivered correctly.
      *Test:* `tests/handoff/test_lead_and_dispatch.py` — dispatched message carries
      the expected `To` and `Cc` headers.

No change required for `runtime/agents/after_sales.py` (LLD-AG-04) or
`LLD-HO-02` — the agent still only collects slots and hands off, and region
resolution already yields the office id.

Commits / PRs:
