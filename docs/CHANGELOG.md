# Changelog

One entry per document version. Newest first.

## 2026-09-05

- **LLD 1.17** — clarification (no CR), reconciling docs with the slot-guard + conversion fix as merged (PR #12; see `changes/slot-guard-conversion-notes.md`). From the live miss (turn 8fdc353f: "gas engine driven" mis-extracted as `lubricated=false`, excluding the correct SCMD-published Natural Gas family; the lone wrong-family match then tempted a 10×-wrong prose SCMD→Nm³/hr conversion, correctly gate-stripped): LLD-AG-01 gains **pre-match token guards** — an LLM-inferred `lubricated`/`standard` survives only if a matching token appears in the visitor's own turns (Latin + Devanagari lubrication tokens, never bare "oil"; standard acronyms case-insensitive but EN/IS/BS/BIS requiring uppercase + adjacent digit so ordinary "is 3000" never validates a hallucinated filter), because the empty-match relaxation retry cannot catch a wrong-but-nonempty match; strips are traced and recorded in the invocation output, and the user-turns-only affirmation limitation is documented as accepted. The persona gains the **no-arithmetic** honesty rule, and LLD-TOOL-01 additively exposes **`converted_capacity {value, unit, from}`** on cross-unit matches so the compose presents the tool-computed reconciliation instead of deriving one. `cap-wellhead-natgas` tightened (`must_not` 20833); suite stays 58. M7 follow-up recorded: the LLD-EXT-04 verbatim-evidence gate generalised to runtime slot extraction, verified across scripts. Gate: pytest 341 green; full three-layer PASS + standalone all-layer 58/58 under the certified thinking-on config; live acceptance 5/5 on the exact miss question.

## 2026-09-04 (presentation shapes)

- **LLD 1.16** — clarification (no CR), reconciling docs with the presentation-shapes pass as merged (PR #11; see `changes/presentation-shapes-notes.md`): the AG-preamble presentation rules gain the two-situation table rule — an **explicit comparison of 2+ named products/families** takes an attribute table (one column per product, cells only from that product's tool results, a row missing for one product omitted entirely, never "Not specified"), 3+ variants/per-type sets keep their table, and prose-siblings is scoped to variant mentions — plus the **clustered-orientation rule** for broad range asks (3–5 clusters, no model lists, no spec figures); LLD-AG-02 gains the `compare_items` parse and the per-item **multi-fetch** (structured lookup or division listing per item, targeted per-item retrieval, per-item force-cite in code; the retrieval co-occurrence path preserved as fallback; cross-division comparisons a known limit); LLD-RT-07 gains the **per-language `LLM_DISABLE_THINKING` semantics** — `hi` always keeps compose thinking (the Devanagari register needs reasoning at this model size), `en`/`hinglish` may run thinking-off — shipped **uncertified, default off** after knob=1 gating flaked a rotating English golden, with the M7 hardening targets (faq_company over-citation, application_discovery completeness, and the `doc.catalogue_testing` release-data pollution — a corpus cleanup item independent of the knob) recorded in the notes; LLD-EVAL-01 suite **55 → 58** with the overview and comparison shape-assertion mechanisms. Gate: full three-layer PASS and standalone all-layer run 58/58 under the certified thinking-on config; live checks green for all three division overviews, the cross-family comparison table and the Hindi overview.

## 2026-09-04

- **LLD 1.15** — clarification (no CR), reconciling docs with the Hindi (Devanagari) support + reply-language fix as merged (PR #10; see `changes/hindi-support-notes.md`): LLD-RT-07 gains the deterministic triage script clause (Devanagari → `hi`, romanised → `hinglish`), the written-Hindi **three-exception register** (Latin script only for exact product/family/model names in bold, units and standard codes; figures always ASCII digits, Indian-grouped; tool vocabulary translated per the persona glossary), the **symmetric last-position `respond_in`** — all three languages get a firm instruction of equal strength closing with the shared *"the reply language comes ONLY from this instruction"* clause, with language-tagged exemplars and the imitate-only-matching-language persona rule (root cause of the turn-b8b63a4f English→Hinglish regression), and the five fixed customer-facing strings (grounding fallback / clarify / hiccup / no-match / price-handoff) as deterministic per-language en/hi/hinglish templates, never LLM-translated at runtime (four English-only residuals documented with triggers); LLD-RT-05 — `ground_answer` ships the fallback in the visitor's language and the numeric-guard extraction folds Devanagari digits ०-९ to ASCII; LLD-EVAL-01 suite **53 → 55** (Hindi e2e pair) with the opt-in `script_purity: hi` zero-threshold Latin-run check (shared checker with the fixed-template unit tests) and romanised-Hindi `must_not_contain` direction guards on three English e2e questions — every reply language now has a golden asserting the other directions do not appear uninvited. Gate: full three-layer PASS over all 55; live checks green in all three directions. `docs/README.md` gains the **branch prompt-activation rule** ("unmerged ≠ not live").

## 2026-09-03 (housekeeping)

- **LLD 1.14** — clarification (no CR), reconciling docs with the housekeeping pass (see `changes/housekeeping-notes.md`): LLD-DB-06 gains migration `0005` — `created_at`/`updated_at` on the seven staging mirrors (+`updated_at` on the RC ledger), maintained at the app UPDATE sites with `clock_timestamp()`, `facts.*` deliberately left timestamp-free (immutable rows; release.built_at + diff is the history), pre-0005 rows carrying migration-time stamps; LLD-EVAL-02 gains the e2e retry-once flaky policy — retry-pass counts for the gate but is always reported (`e2e 53/53, N flaky`) with first-attempt evidence, two failures block, deterministic layers never retry; LLD-RT-05/EVAL-01 — a gate-stripped turn records `outcome=fallback` (countable in ops monitoring) and the eval vocabulary carries the value, golden audit clean. The dormant compose-only `LLM_DISABLE_THINKING` knob ships measurement-only (default off): knob-on suite three-layer green, compose latency/output tokens ~10× lower, whole-turn latency ~halved, 2 flaky absorbed by the retry policy — adoption deferred to milestone 7 with the numbers recorded in the notes.

## 2026-09-03

- **LLD 1.13** — clarification (no CR), reconciling docs with the sources-resolver + tracing task as merged (see `changes/sources-and-tracing-notes.md`): LLD-RT-05 gains the customer-facing sources rider — `resolve_sources` walks every citation to its source document via the `active_*` views and renders one grouped entry per document (`title / url / link / location`), PDF locators becoming page-anchored `#page=N` links, null titles taking the URL-derived label, null urls rendered without a link; the list is merged into the answer message payload (`ops.message.payload.sources`, JSONB, no schema change) so the conversation record keeps exactly what the customer was shown, and the resolver is fail-open. Raw `ops.citation` rows remain the audit trail; TRACEABILITY PRD-F-008 updated in the task itself. Langfuse tracing (external self-hosted v4 instance, dev-gated behind `JYOTECH_DEV_UI`, fail-open, force-off on eval/prompt-activate/release-activate) is dev tooling with no LLD item — documented in the changes note only.

## 2026-09-02

- **LLD 1.12** — clarification (no CR), reconciling docs with the post-merge polish pass (see `changes/polish-pass-notes.md`): LLD-RT-02 gains the optional read-only `on_stage` progress callback (inert when unset; consumed by the dev harness, reused by the milestone-7 SSE stage events); LLD-RT-05 citation derivation generalised to `used_documents` — every document the answer actually drew on is cited, not only the best chunk; the LLD-AG preamble gains the presentation rules (bold display names, tables only at 3+ variants or per-type spec sets, figures never separated from their units — a numeric-guard constraint) and the **source-voice rule** (prose speaks as Jyotech, never names documents/catalogues/pages; provenance rides only in structured citations; exceptions for document-download requests and direct source questions); LLD-AG-03 serves one `document_card` per distinct downloadable document the answer references (file-URL filter, capped at 3, prose tied to the card list); LLD-EVAL-01 suite at **53** with source-voice `must_not_contain` guards on three non-document questions and the two-catalogue citation golden; LLD-EVAL-03 records the batch-bump gate pattern (partial bring-ups, full three-layer gate on the final activation over the complete set, standalone all-layer PASS). Live: e2e-green over all 53; source-voice verified in EN and Hinglish through the dev harness.

## 2026-08-31

- **LLD 1.11** — clarification (no CR), reconciling docs with milestone 5b as merged (see `changes/milestone-5b-notes.md`): all six in-scope intents routed to their agents, deflect reserved for out-of-scope (LLD-RT-03); triage routing clarified — machine standards-compliance → `documents`, company certs and export-coverage → `faq` (LLD-RT-04); grounding gate hardened from the turn-271ff0e2 regression — the numeric guard now runs on **every outgoing message whatever the action**, stripped figures recorded in `turn.grounding`; citations enriched with the honest parent of each cited result, pinned to the chunk the answer actually used, trimmed to the rows the answer reflects; false-memory restatement rule (LLD-RT-05); per-turn response language + English retrieval-query translation and the Hinglish register (LLD-RT-07); shared persona block, **absence protocol** (search before any "not published" claim), exemplar placeholder figures, Indian-style number formatting (LLD-AG preamble); structural slot-complete rule + force-cited match (LLD-AG-01); product_advisor structured-plus-retrieval compose and variants presentation, document_card on the gated answer path with URL-derived title fallback, after-sales `contact_detail` slot (lead row still M6), number-free commercial template, faq_company multi-part decomposition (LLD-AG-02..06); same-unit family ranking (LLD-TOOL-01), get_product sibling-variant matching (LLD-TOOL-03), chunk titles (LLD-TOOL-04); golden suite at **52** with `must_not_contain`, digit-typography-insensitive matching and the single-turn outcome rule (LLD-EVAL-01), the e2e layer running the real orchestrator in a rolled-back savepoint (LLD-EVAL-02), and prompt activation gated on all three layers by default with `--layers` demoted to bring-up-only (LLD-EVAL-03). Live: fact 40/40 · retrieval 6/6 · e2e green at 52; hardened against the gate-label loophole, the absence false-negatives (EIGA, founder, API-618) and the wellhead cross-family recitation.

## 2026-08-29 (runtime)

- **LLD 1.10** — clarification (no CR), reconciling docs with milestone 5a as merged (see `changes/milestone-5a-notes.md`): §7 preamble — prompts versioned in `ops.prompt_version`, `prompt activate` gated by the golden suite, `agent_invocation` records its prompt_version; LLD-RT-01 CLI chat loop now, API/SSE at the widget milestone; LLD-RT-02 thin in-memory StateGraph with explicit ops-insert persistence (write-before-emit), Postgres checkpointer deferred to the widget milestone, ops rows the single source of truth; LLD-RT-05 grounding gate as built — ≥1 resolving citation **plus the numeric guard** (unsourced number = uncited → strip + fallback), per-claim splitting deferred; LLD-TOOL-01 result reshaped from the live hydrogen fix cycle — `matches` (per-match headroom/near_edge/caveats, comparable-filter failures excluded) vs `non_comparable_candidates` (reasoned, never presented as matches), gas alias map / null-lubricated caveat / extended unit vocabulary implemented; LLD-AG-01 the row is the authority for numbers, retrieval scoped to the top match's family for supporting prose only, customer-facing naming rules. First live conversations verified end to end (hydrogen §4 trace, slot question, FAQ, deflection, break-in attempts held, session resume).

## 2026-08-29

- **LLD 1.9** — clarification (no CR), reconciling docs with milestone 4 as merged (see `changes/milestone-4-notes.md`): LLD-RET-01 mechanical family tagging (active facts rows + name matches, never an LLM) and derived chunk division; LLD-RET-02 real bge-m3 tokenizer via `embed_tokenizer`/`embed_tokenizer_path` and the truncation canary pinned by locator; LLD-RET-04 implemented — disposition in `clients/<client>/seeds/chunking.yaml` with `agentkit chunk run` as a human STOP gate; LLD-TOOL preamble — `ops.tool_call` logging deferred until `ops.*` exists; LLD-EVAL-01 suite frozen at 46 questions with `fact:`/`retrieval:` execution blocks and the id-maintenance rule; LLD-EVAL-03 golden gate wired to `release activate` (rollback on failure; embedding best-effort at promote). Live: 138 chunks, 138/138 embedded to `vec.chunk_embedding_r2026_08_2`, eval PASS (fact 39/39 scored, retrieval 6/6 scored, e2e pending agents).
- **LLD 1.8** — clarification (no CR), reconciling docs with milestone 3b as merged (see `changes/milestone-3b-notes.md`): `capability_gas` carries no source columns by design — provenance inherited via the composite parent FK, gated relationally at promote (LLD-DB-02, LLD-REL-05, data-model §2.4); `facts.product.model_name` nullable when the catalogue prints no model — migration `0003`, never invented, runtime presents family + variant (LLD-EXT-06, data-model §2.3); LLD-REL-03 rewritten as the implemented integrity check (hard: closure, provenance, unit-on-numerics, duplicates, `needs_family`, schema-driven NOT-NULL safety net, office→`region_state` resolution with printed mapping; warnings: orphan families, kept `conflict_group`s); LLD-REL-05 promote details (provenance scoping, multi-`;` families.yaml source parsing, `rYYYY.MM.N` ids); LLD-TOOL-01 non-comparable-unit rule now, plus the deferred design note (gas alias map, null-boolean semantics, normalise vocabulary extension). First real release **r2026.08.2** promoted and active.

## 2026-08-28

- **LLD 1.7** — clarification (no CR), reconciling docs with milestone 3a as merged (see `changes/milestone-3a-notes.md`): LLD-DB-06 gains the `0002` staging additions — `conflict_group` / `needs_family` / `section_id` on all mirrors and the `staging.release_candidate` ledger (`open/exported/imported/promoted/abandoned`), with the deliberate no-FK stance (RC validity enforced at promote); LLD-EXT §3 sampling is provider-configurable (knobs sent only when set; strict json_schema decoding is the faithfulness mechanism) with per-call logging and prompt-cache-aware token/cost reporting; LLD-EXT-01/02 section split + file artifacts under `data/<client>/extract/<rc>/`; LLD-EXT-03 families.yaml human stop + frozen-family (`needs_family`, never invented ids); LLD-EXT-04 code-level verbatim evidence gate (`extract/evidence.py`, drop + log to `drops.json`); LLD-EXT-09 "X to Y" ranges; LLD-EXT-10 RC idempotency (replace pending only) + file-based prompts; LLD-REL-01 export as built (evidence-beside-value, decision dropdown, RC → `exported`) + RC lifecycle; LLD-REL-02 decision semantics (blank = not promoted; rejected rows' children excluded); LLD-REL-05 promote gate — refuse RC without ledger row or wrong status — and `facts.product_family` populated from approved `families.yaml`.
- **PRD 1.2 · HLD 1.2 · LLD 1.6** — CR-0002 (`changes/CR-0002.md`), propagated: PRD-N-002 scoped — self-hosted-model + no-customer-data-egress applies to the customer-facing runtime; the offline content-extractor may use an external API LLM on public content only (embeddings stay self-hosted). HLD-005 scoped + HLD-006/§5 data-egress notes; LLD endpoints preamble adds `EXTRACT_LLM_BASE_URL` (external-capable, defaults to `LLM_BASE_URL`) with an LLD-EXT §3 note; `config/` + `extract/` gain the extractor endpoint (new test `tests/extract/test_extractor_endpoint.py`). TRACEABILITY realigned PRD 1.2 → HLD 1.2 → LLD 1.6. Tag `docs/prd-v1.2` after commit.

## 2026-08-23

- **PRD 1.1 · HLD 1.1 · LLD 1.5** — CR-0001 (`changes/CR-0001.md`), propagated: after-sales routes to the published branch-office email for the customer's region, copying sales@ (PRD-F-006 modified; assumption A-1 updated). HLD-C-08 region-routing wording refined; **LLD-HO-03** recipient selection reworked (after-sales `to` = branch-office email via `region_state.office_id`, `cc` = sales@, fallback sales@) — LLD-HO-04 mailer gains `cc`. TRACEABILITY realigned PRD 1.1 → HLD 1.1 → LLD 1.5. Tag `docs/prd-v1.1` after commit.
- **LLD 1.4** — clarification (no CR), reconciling docs with milestone 2 as merged (see `changes/milestone-2-notes.md`): LLD-ING-01 corrected from "11 HTML URLs" to the sources.yaml-driven list (27 pages + 2 PDFs) with human-decided `exclude:`; cleaner rules in ING-02; pypdfium backend, ACCURATE table mode and tidy pass in ING-03; staging-only writes under the bootstrap RC in ING-05; **new LLD-ING-06**: `agentkit ingest verify` (independent pdftotext token witness; requires poppler — `brew install poppler` / `apt install poppler-utils`); **new LLD-RET-04** design note for the chunking milestone (per-document disposition + corpus-wide paragraph dedup).
- **LLD 1.3** — clarification (no CR), reconciling the docs with milestone 1 as merged (see `changes/milestone-1-notes.md`): composite `(natural_id, release_id)` keying, one-active-release partial unique index and views-only access in LLD-DB-02; staging narrowed to the seven content tables with no cross-table FKs in LLD-DB-06; new LLD-DB-07 for `region_state` as curated, source-less configuration; headroom formula in LLD-TOOL-01; SCMD constant (1 Nm³/hr = 24 SCMD) in LLD-EXT-09. `design/data-model.md` corrections (keying-rule note, `fam.search_eq`, `doc.index`/`doc.contact`) were made in the milestone-1 commit itself.

- **LLD 1.2** — clarification (no CR): LLD-DB-01 now records the `0000_bootstrap` migration (vector extension + four empty schemas, reversible) preceding `0001_init`, and that DB state belongs to migrations, never docker init SQL. Decision confirmed during Step 0.
- **LLD 1.1** — clarification (no CR): LLD-RET-01/02 now require an explicit embed context window (`num_ctx` for Ollama), a loud `ChunkTooLargeError` instead of silent truncation, the split-long-tables-by-rows rule, and a truncation-canary golden question. Process rule 6 (LLD clarification path) added to docs/README.md.

- **PRD 1.0** — initial approved requirements for Iteration 1 (public content only). Tag `docs/prd-v1.0`.
- **HLD 1.0** — aligned to PRD 1.0. Decisions HLD-001…009, components HLD-C-01…10.
- **LLD 1.0** — aligned to HLD 1.0 / PRD 1.0.
- **TRACEABILITY** — generated.
